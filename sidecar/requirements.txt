# FOUNDRY CAD sidecar — runtime deps
fastapi>=0.110
uvicorn>=0.29

# CSG enclosure builder (real boolean cutouts + standoffs). enclosure.py falls back to
# the dependency-free box shell if these are missing, so the sidecar always runs.
trimesh>=4.4
manifold3d>=2.5
numpy>=1.26
shapely>=2.0
networkx>=3.0   # required by trimesh's 3MF exporter
lxml>=5.0       # required by trimesh's 3MF exporter
